# server
Here lays all the docker-compose.yml files and more to configure Caddy and crowdsec to work together to protect Immich and Nextcloud, each service running in its own container. only debian is bare metal.
